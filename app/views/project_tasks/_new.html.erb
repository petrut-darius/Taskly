<%= form_with model: [ @project, project_task], local: true do |form| %>
  <% if project_task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project_task.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
